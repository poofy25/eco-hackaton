import { Star } from "lucide-react";
import { testimonials, partnerLogos } from "@/lib/mock-data"; export default function SocialProof() { return ( <section className="py-16 sm:py-24 bg-white"> <div className="mx-auto max-w-7xl px-4 lg:px-6"> {/* Partner logos */} <div className="flex flex-wrap items-center justify-center gap-8 sm:gap-12"> {partnerLogos.map((name) => ( <span key={name} className="font-heading text-sm sm:text-base font-semibold text-stone/40 whitespace-nowrap" > {name} </span> ))} </div> {/* Testimonials */} <div className="mt-16 grid grid-cols-1 md:grid-cols-3 gap-6"> {testimonials.map((t) => ( <div key={t.name} className="rounded-xl bg-white p-6 border border-stone-200" > <div className="flex gap-0.5 mb-4"> {[...Array(5)].map((_, i) => ( <Star key={i} className="h-4 w-4 fill-amber-400 text-amber-400" /> ))} </div> <p className="text-sm text-charcoal leading-relaxed"> &ldquo;{t.quote}&rdquo; </p> <div className="mt-5 flex items-center gap-3"> <img src={t.avatar} alt={t.name} className="h-10 w-10 rounded-full object-cover" /> <div> <p className="text-sm font-semibold text-charcoal"> {t.name} </p> <p className="text-xs text-stone">{t.company}</p> </div> </div> </div> ))} </div> {/* Fee callout */} <div className="mt-12 text-center"> <p className="text-sm text-stone"> Salvio percepe un comision mic doar atunci când realizezi o vânzare.{" "} <span className="font-medium text-charcoal"> Listarea este întotdeauna gratuită. </span> </p> </div> </div> </section> );
}
