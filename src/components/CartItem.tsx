"use client"; import Link from "next/link";
import { Minus, Plus, Trash2 } from "lucide-react";
import type { CartItem as CartItemType } from "@/lib/cart-context";
import { useCart } from "@/lib/cart-context";
import { formatPrice } from "@/lib/utils"; interface CartItemProps { item: CartItemType;
} export default function CartItem({ item }: CartItemProps) { const { updateQuantity, removeItem } = useCart(); const { listing, quantity } = item; const lineTotal = listing.price * quantity; return ( <div className="flex gap-4 py-4"> <Link href={`/listing/${listing.id}`} className="h-20 w-20 shrink-0 overflow-hidden " > <img src={listing.images[0]} alt={listing.title} className="h-full w-full object-cover" /> </Link> <div className="flex flex-1 flex-col justify-between min-w-0"> <div> <Link href={`/listing/${listing.id}`} className="font-heading text-sm font-semibold text-mercury-900 line-clamp-1 hover:text-mercury-900 transition-colors" > {listing.title} </Link> <p className="text-xs text-mercury-500 mt-0.5"> {listing.seller.name} · {listing.location} </p> </div> <div className="flex items-center justify-between mt-2"> <div className="flex items-center -500-200"> <button onClick={() => updateQuantity(listing.id, quantity - 1)} className="p-1.5 hover:bg-mercury-50 transition-colors -lg" aria-label="Decrease" > <Minus className="h-3 w-3 text-mercury-500" strokeWidth={1.5} /> </button> <span className="px-3 text-sm font-medium text-mercury-900"> {quantity} </span> <button onClick={() => updateQuantity(listing.id, Math.min(listing.quantity, quantity + 1))} className="p-1.5 hover:bg-mercury-50 transition-colors -lg" aria-label="Increase" > <Plus className="h-3 w-3 text-mercury-500" strokeWidth={1.5} /> </button> </div> <div className="flex items-center gap-3"> <span className="font-heading text-sm font-bold text-mercury-900"> {listing.isFree ? "FREE" : formatPrice(lineTotal)} </span> <button onClick={() => removeItem(listing.id)} className="p-1.5 hover:bg-red-50 text-mercury-500 hover:text-red-600 transition-colors" aria-label="Remove" > <Trash2 className="h-4 w-4" strokeWidth={1.5} /> </button> </div> </div> </div> </div> );
}
