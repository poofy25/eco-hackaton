"use client"; import ListingCard from "@/components/ListingCard";
import { listings } from "@/lib/mock-data";
import { Bell } from "lucide-react"; export default function SavedItemsPage() { const savedListings = listings.filter((_, i) => [2, 4, 5, 7, 11, 13].includes(i)); return ( <div> <h1 className="font-heading text-2xl font-bold text-mercury-900 mb-8"> Articole Salvate </h1> <div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4 sm:gap-6 mb-12"> {savedListings.map((listing) => ( <ListingCard key={listing.id} listing={listing} /> ))} </div> {/* Saved Searches */} <h2 className="font-heading text-lg font-semibold text-mercury-900 mb-4"> Căutări Salvate </h2> <div className="space-y-3"> {[ { query: "Cherestea brad Douglas", filters: "Surplus Nou, < 25 km" }, { query: "Panouri LED", filters: "Orice stare, < 50 km" }, { query: "Gresie pardoseală", filters: "Doar gratuite" }, ].map((search) => ( <div key={search.query} className="flex items-center justify-between bg-white p-4 -300" > <div> <p className="text-sm font-medium text-mercury-900"> &ldquo;{search.query}&rdquo; </p> <p className="text-xs text-mercury-500 mt-0.5">{search.filters}</p> </div> <button className="flex items-center gap-1.5 -500/20 px-3 py-1.5 text-xs font-medium text-mercury-900 hover:bg-white transition-colors"> <Bell className="h-3 w-3" strokeWidth={1.5} /> Alerte Activate </button> </div> ))} </div> </div> );
}
