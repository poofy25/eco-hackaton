"use client"; import ListingCard from "@/components/ListingCard";
import { listings } from "@/lib/mock-data";
import { Bell } from "lucide-react"; export default function SavedItemsPage() { const savedListings = listings.filter((_, i) => [2, 4, 5, 7, 11, 13].includes(i)); return ( <div> <h1 className="font-heading text-2xl font-bold text-black-900 mb-8"> Saved Items </h1> <div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4 sm:gap-6 mb-12"> {savedListings.map((listing) => ( <ListingCard key={listing.id} listing={listing} /> ))} </div> {/* Saved Searches */} <h2 className="font-heading text-lg font-semibold text-black-900 mb-4"> Saved Searches </h2> <div className="space-y-3"> {[ { query: "Douglas Fir lumber", filters: "New Surplus, < 25 mi" }, { query: "LED panel lights", filters: "Any condition, < 50 mi" }, { query: "Tile flooring", filters: "Free only" }, ].map((search) => ( <div key={search.query} className="flex items-center justify-between rounded-none bg-white p-4 border border-black-300" > <div> <p className="text-sm font-medium text-black-900"> &ldquo;{search.query}&rdquo; </p> <p className="text-xs text-black-500 mt-0.5">{search.filters}</p> </div> <button className="flex items-center gap-1.5 rounded-none border border-black-500/20 px-3 py-1.5 text-xs font-medium text-black-900 hover:bg-white transition-colors"> <Bell className="h-3 w-3" strokeWidth={1.5} /> Alerts On </button> </div> ))} </div> </div> );
}
