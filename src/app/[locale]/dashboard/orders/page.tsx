"use client"; import { orders } from "@/lib/mock-data";
import { formatPrice, formatDate } from "@/lib/utils"; const statusColors: Record<string, string> = { pending: "bg-amber-100 text-amber-700", confirmed: "bg-blue-100 text-blue-700", ready: "bg-mercury-500/10 text-mercury-500", completed: "bg-mercury-900/10 text-mercury-900", cancelled: "bg-mercury-500/10 text-mercury-500",
}; const statusLabels: Record<string, string> = { pending: "în așteptare", confirmed: "confirmat", ready: "pregătit", completed: "finalizat", cancelled: "anulat",
}; const logisticsLabels: Record<string, string> = { pickup: "ridicare", delivery: "livrare", shipping: "expediere",
}; export default function OrdersPage() { return ( <div> <h1 className="font-heading text-2xl font-bold text-mercury-900 mb-8"> Comenzi </h1> <div className="bg-white -500-200 overflow-hidden"> <div className="overflow-x-auto"> <table className="w-full"> <thead> <tr className="-500-200 bg-mercury-50/30"> <th className="px-5 py-3 text-left text-xs font-medium text-mercury-500 uppercase tracking-wider"> Nr. Comandă </th> <th className="px-5 py-3 text-left text-xs font-medium text-mercury-500 uppercase tracking-wider"> Cumpărător </th> <th className="px-5 py-3 text-left text-xs font-medium text-mercury-500 uppercase tracking-wider"> Articole </th> <th className="px-5 py-3 text-left text-xs font-medium text-mercury-500 uppercase tracking-wider"> Data </th> <th className="px-5 py-3 text-left text-xs font-medium text-mercury-500 uppercase tracking-wider"> Logistică </th> <th className="px-5 py-3 text-left text-xs font-medium text-mercury-500 uppercase tracking-wider"> Status </th> <th className="px-5 py-3 text-right text-xs font-medium text-mercury-500 uppercase tracking-wider"> Sumă </th> </tr> </thead> <tbody className="divide-y divide-mercury-500-200"> {orders.map((order) => ( <tr key={order.id} className="hover:bg-mercury-50/30 transition-colors cursor-pointer" > <td className="px-5 py-4 font-data text-sm text-mercury-900"> {order.orderNumber} </td> <td className="px-5 py-4"> <div className="flex items-center gap-2.5"> <img src={order.buyer.avatar} alt={order.buyer.name} className="h-7 w-7 object-cover" /> <div> <p className="text-sm font-medium text-mercury-900"> {order.buyer.name} </p> <p className="text-xs text-mercury-500">{order.buyer.email}</p> </div> </div> </td> <td className="px-5 py-4"> <p className="text-sm text-mercury-900 line-clamp-1"> {order.items.map((i) => i.listing.title).join(", ")} </p> <p className="text-xs text-mercury-500"> Cant: {order.items.reduce((s, i) => s + i.quantity, 0)} </p> </td> <td className="px-5 py-4 text-sm text-mercury-500 whitespace-nowrap"> {formatDate(order.createdAt)} </td> <td className="px-5 py-4 text-sm text-mercury-900 capitalize"> {logisticsLabels[order.logistics] || order.logistics} </td> <td className="px-5 py-4"> <span className={`inline-block px-2.5 py-0.5 text-xs font-medium capitalize ${ statusColors[order.status] || "bg-mercury-500/10 text-mercury-500" }`} > {statusLabels[order.status] || order.status} </span> </td> <td className="px-5 py-4 text-right font-data text-sm font-medium text-mercury-900"> {formatPrice(order.total)} </td> </tr> ))} </tbody> </table> </div> </div> </div> );
}
