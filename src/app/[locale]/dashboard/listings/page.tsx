"use client"; import { useState } from "react";
import Link from "next/link";
import { PlusCircle, Eye, Heart, Pause, Play, Trash2, Edit } from "lucide-react";
import { listings } from "@/lib/mock-data";
import { formatPrice, formatDate, cn } from "@/lib/utils"; const statusFilters = ["Toate", "Active", "Pauzate", "Vândute"]; const statusFilterMap: Record<string, string> = { "Active": "active", "Pauzate": "paused", "Vândute": "sold", }; const statusBadge: Record<string, string> = { active: "bg-mercury-500/10 text-mercury-500", paused: "bg-amber-100 text-amber-700", sold: "bg-mercury-500/10 text-mercury-500",
}; const statusLabels: Record<string, string> = { active: "activ", paused: "pauzat", sold: "vândut",
}; export default function MyListings() { const [filter, setFilter] = useState("Toate"); // Use first 8 listings as seller's own const myListings = listings.slice(0, 8); const filtered = filter === "Toate" ? myListings : myListings.filter( (l) => l.status === statusFilterMap[filter] ); return ( <div> <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-6"> <h1 className="font-heading text-2xl font-bold text-mercury-900"> Anunțurile Mele </h1> <Link href="/dashboard/new-listing" className="btn-magnetic btn-cta flex items-center gap-2 bg-mercury-500 px-5 py-2.5 text-sm font-semibold text-white" > <PlusCircle className="h-4 w-4" strokeWidth={1.5} /> Anunț Nou </Link> </div> {/* Filters */} <div className="flex gap-2 mb-6"> {statusFilters.map((f) => ( <button key={f} onClick={() => setFilter(f)} className={cn( "px-4 py-1.5 text-sm font-medium transition-colors", filter === f ? "bg-[#FED601] text-mercury-900 py-2 -900" : "bg-white text-mercury-500 hover:bg-white -500/15" )} > {f} </button> ))} </div> {/* Listings Grid */} <div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4"> {filtered.map((listing) => ( <div key={listing.id} className="bg-white p-4 -900" > <div className="flex gap-3"> <img src={listing.images[0]} alt={listing.title} className="h-20 w-20 object-cover shrink-0" /> <div className="flex-1 min-w-0"> <h3 className="text-sm font-semibold text-mercury-900 line-clamp-2"> {listing.title} </h3> <div className="mt-1 flex items-center gap-2"> <span className={`px-2 py-0.5 text-[10px] font-medium capitalize ${ statusBadge[listing.status] }`} > {statusLabels[listing.status]} </span> <span className="text-xs text-mercury-500 font-medium"> {listing.isFree ? "GRATUIT" : formatPrice(listing.price)} </span> </div> </div> </div> <div className="mt-3 flex items-center gap-4 text-xs text-mercury-500"> <span className="flex items-center gap-1"> <Eye className="h-3 w-3" strokeWidth={1.5} /> {listing.viewCount} </span> <span className="flex items-center gap-1"> <Heart className="h-3 w-3" strokeWidth={1.5} /> {listing.savedCount} </span> <span className="ml-auto">Listat {formatDate(listing.listedAt)}</span> </div> <div className="mt-3 pt-3 -500/10 flex gap-2"> <button className="flex items-center gap-1 px-2.5 py-1.5 text-xs font-medium text-mercury-900 hover:bg-white transition-colors -500/15"> <Edit className="h-3 w-3" strokeWidth={1.5} /> Editează </button> <button className="flex items-center gap-1 px-2.5 py-1.5 text-xs font-medium text-mercury-900 hover:bg-white transition-colors -500/15"> {listing.status === "active" ? ( <><Pause className="h-3 w-3" strokeWidth={1.5} /> Pauză</> ) : ( <><Play className="h-3 w-3" strokeWidth={1.5} /> Activează</> )} </button> <button className="flex items-center gap-1 px-2.5 py-1.5 text-xs font-medium text-mercury-500 hover:bg-mercury-500/5 transition-colors -500/15"> <Trash2 className="h-3 w-3" strokeWidth={1.5} /> Șterge </button> </div> </div> ))} </div> </div> );
}
